# 変更履歴

## 2026-09-22 — 公開前の精査（個人情報除去・ライセンス）

- **個人情報/固有識別情報を一般表記へ**: 個人環境の実ユーザー名・実IP・実モデル名・unit 名を
  `YOUR_USER` / `YOUR_GPU_SERVER` / `SomeModel-Q4_K_M` / `llama-server.service` に置換、
  未使用定数 `TITLE`（自サーバの愛称）削除。
- **参考元 OSS の帰属**: シリアルプロトコル（6byte ヘッダ、コマンド値、RGB565、HELLO）は
  `mathoudebine/turing-smart-screen-python`（**GPL-3.0-or-later**）の `lcd_comm_rev_a.py` を検証基準に使用。
  実装はこのリポジトリで独立記述だがプロトコル形式の派生として **LICENSE に GPL-3.0 全文を同梱**し README に帰属明記。
- **再配布しないもの**: Turing 公式ソフト（USBMonitor.exe / ExtendScreen.exe 等）は取得・同梱せず、
  挙動検証の参照元としてのみ言及。
- **プレビュー画像の差し替え**: `assets/preview_latest.png` は実サーバのダッシュボードスナップショットで
  モデル名が画面に写っていた。合成データ専用の `tools/gen_preview.py` で生成し直した差し替え画像に更新。
- `install_root.sh` を相対パス化（任意のインストール先で動作）。
- **README の中国語混入を修正**（サンプリング/サンプル 系語 4か所。プレビュー系の混入も別途修正済）。

## 2026-09-22 — GPUカードにGPU名を表示

- **GPU名表示**: カードタイトルを `GPU 0` → `GPU 0 (Tesla V100-PCIE-32GB)` に変更。
  名前は `nvidia-smi` の `name` フィールド（既存 QUERY に含まれる）から取得し、
  静的な `card{k}` リージョンに織り込む。フォントは左揃えで pstate 手前（`w - S(46)`）
  まで自動縮小しクリップしない。`name` 欠落時は `GPU k` のみにフォールバック。
  名前導入によりカード枠は起動時 1 回だけ再送（約14KB/カード、以後は不動）。
- `--demo` 合成データに `name` を追加（dry-run レイアウト検証対応）。

## 2026-09-22 — 色分け・モデル表示・枠線クリップ修正

- **PWR のフォント色**: ≤80W 緑 / ≤150W 黄 / >150W 赤（`POWER_WARN, POWER_CRIT`）。
  dry-run で緑（30W/35W）と赤（165W/200W）を実測確認。実負荷での黄検証は省略（ユーザー指示）。
- **CORE/MEM のフォント色**: <58℃ 緑 / <70℃ 黄 / ≥70℃ 赤（`TEMP_WARN, TEMP_CRIT`）。
  MEM は固定色だった分岐を撤廃し CORE と共通化。
- **単位を ℃ に変更**（U+2103 単一グリフ、DejaVu に存在）。
- **グラフの縦軸下限を 40℃ に**（`TEMP_SCALE_MIN=40`）。40℃ 未満はほぼ来ないため表示領域を拡大。
  目盛りは自動間引き（スパーク 40/80、トレンド 40/60/80）。
- **3桁 PWR 対応**: フォントサイズを worst-case 文字列（`255W`）から固定決定。
  2桁→3桁でサイズが変わらない（ジッターなし）。実測 125W/140W でクリップなしを確認。
- **モデル名表示**: `llama-server` の `--model` を `/proc/<pid>/cmdline` から抽出、
  `<repo(-GGUF)>-<quant>` に変換して下部パネルにボールド17pxで表示。
  実プロセスでの抽出を動作確認。
- **ComfyUI 表示**: `/proc` 走査で `ComfyUI/main.py` を検出、● ACTIVE（緑）/ ● OFF（赤）。
- **llama 判定の変更**: プロセス検索のみに。systemd unit 参照は複数サービスの使い分けで
  正しくならないため主基準から撤廃（fallback としても残さない方向、現状は llama_unit 定数のみ残存）。
- **枠線クリップの修正**（背景塗りつぶしが枠線を塗り消していた）:
  - GPU カードの CORE/MEM/PWR 読み取り部 → 内側左右2px
  - 下部パネルのモデル表示 → 左右2px
  - 下部パネルのクロック表示 → 左右2px（幅 230→226）
- **ヘッダ廃止**（タイトルバーと時計）。閾値線がヘッダと重なって見えなかったため。
- **パネル間 gap=0**（枠線共有）。白い隙間が背景色として目立ったため。
- **CORE/MEM/PWR を同サイズの横並び3列**に（余白解消）。
- **ステータスパネルの重なり修正**: `AVG UTIL` と llama ドットの衝突を列配置で解消。
- **P0 タグの右端クリップ修正**（右揃え位置に内側余白）。
- 位置検証アサーション拡張: ステータスパネル内の全ウィジェット同士の非重複を検査。

## 2026-09-21 — 接続・向き・キャンバスの確定

- 実機は Turing/UsbMonitor 3.5"、HELLO 無応答と確認。
- `SET_ORIENTATION`（16byte フレーム）送信後はビットマップが映らない → 送信しない方針。
- 320x480 ビットマップは横幅480のバッファに行折り返しで流し込まれる（AB/CD が重なる症状）→
  キャンバスは 480x320・向きコマンドなしで正しく枠内に収まることを 4象限プローブで確認。
- 受信ビットマップは 180 度回転して表示される → 送信前に 180 度回転（`FLIP180`）で補償、正位置確認。
- udev rule（`99-turing-screen.rules`, MODE 0666）で sudo なしの書込を可能に。
- `gpu-dash.service`（`User=<サービス実行者>` / `Group=dialout` / `Restart=always`）導入・enabled。
- 差分送信設計（全面 307,200B = 約27s @115200baud は 5s リフレッシュ不能）を確定。
